@page "{id}"
@using FredsWorkmate.Database.Models
@using FredsWorkmate.Util.HtmlHelperExtensions
@model CompanyInformationDetails
@{
    ViewData["Title"] = "Details Of CompanyInformation";
    Html.EntityDetails<CompanyInformation>(Model.Entity);
    Html.DetailsNavigation(Model.Entity.Id);
}